main()
{
    int x , y , r;
    printf("entre com 2 numeros: " );
    scanf("%d %d ",&x,&y);
    r=soma(x,y);
    printf("a soma é : %d " ,r)
}

/*soma()*/
/*retorna a soma dos 2 numeros*/
soma(j,k)
int j , k;
{
    return(j+k);
}